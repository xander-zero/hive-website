/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["imgur.com", "https://web-cdn.snapp.ir/snapp-website/icons/snappTextLogo.svg"],
  },
}

module.exports = nextConfig
